# Environment variables — never commit this
.env

# Per-user API key database — contains sensitive data, never commit
*.db

# Dependencies
node_modules/

# macOS
.DS_Store

# Logs
*.log
npm-debug.log*
